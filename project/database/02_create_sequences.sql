-- Campus Lost & Found Network
-- Sequences Creation Script (PostgreSQL)
-- PostgreSQL uses SERIAL for auto-increment, so explicit sequences are not needed
-- This file is kept for compatibility but sequences are handled by SERIAL types

-- Note: PostgreSQL SERIAL types automatically create sequences
-- No manual sequence creation required

SELECT 'PostgreSQL uses SERIAL for auto-increment. No manual sequences needed.' AS Status;
